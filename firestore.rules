rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST NUCLEARE: Accesso libero per debug.
    // Se l'errore persiste, il problema risiede nella configurazione del client o nel caching del browser.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
