rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST NUCLEARE: Accesso libero per debug
    // Se l'errore persiste con questa regola, il problema risiede nel client o nella configurazione del progetto.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}